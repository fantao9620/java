1.集合框架
  为什么要用集合：存储更为复杂的数据结构
  之前数组存储的是长度固定
2.集合主要架构
  collection接口-------Set接口  HashSet
                                TreeSet
  set存储特点（无序、不重复），常用方法：add()、size()
	用iterator迭代遍历set集合
  List存储特点（有序、可重复）
		       List接口  ---------ArrayList
	ArrayList  地址连续，长度可扩充，随机取数据效率高
		             ---------LinkedList
	LinkedList  地址不连续，增删改效率比较高
ArrayList与LinkedList的比较
存储结构
ArrayList是线性顺序存储 
LinkedList对象间彼此串连起来的一个链表
操作性能
ArrayList适合随机查询的场合
LinkedList元素的插入和删除操作性高
从功能上，LinkedList要多一些

			     ---------Vector（线程安全）

  Map接口------   HashMap    ---一般和HashTable进行对比
                  TreeMap
  HashMap和HashTable区别（下午技术分享）
3.Iterator（迭代器）：遍历set集合
有方法hasNext()判断后续是否有数据
next()方法是读取后续的数据
4.泛型（jdk1.7之后出来的）---约束集合的数据类型
集合类型<数据类型>  对象 = new 集合实现类<>();
4.1 限制加入数据的类型
4.2 读取数据不需要强制转换
